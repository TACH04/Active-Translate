# Example Environment Variables
# Copy this file to .env and fill in the values

# Currently, the app runs entirely locally.
# If you add external services (like OpenAI for translation), add keys here.
# OPENAI_API_KEY=your_key_here
